# Copy to .env and adjust as needed.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Backend API base URL for inspections / products / claims (Render service `AuditX-111`).
# Defaults to https://auditx-111.onrender.com when unset.
VITE_AUDITX_API_URL=
